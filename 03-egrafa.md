# Παραγωγή εγγράφων & ζωντανό περιβάλλον

Τρίτο στάδιο του έργου: το ζωντανό Supabase project, η Edge Function που παράγει τα
έγγραφα του φακέλου από τα πρότυπα της Υπηρεσίας, και η σύνδεσή της με το front-end.

---

## 1. Ζωντανό περιβάλλον

| | |
|---|---|
| Project | `epivlepsi-4412` (ref `nubxaclmwuhqkvuvmvos`) |
| URL | `https://nubxaclmwuhqkvuvmvos.supabase.co` |
| Περιοχή | eu-central-1 (Φρανκφούρτη) |
| Κόστος | 0 € / μήνα (free plan) |
| Migrations | 22 εφαρμοσμένα |

Το ανώνυμο κλειδί (anon key) λαμβάνεται από **Project Settings → API**. Είναι κλειδί
για τον browser· η προστασία των δεδομένων ασκείται εξ ολοκλήρου από τα RLS policies.
Το service-role κλειδί δεν χρησιμοποιείται πουθενά στην εφαρμογή.

Δοκιμαστικοί λογαριασμοί (migration `0020_demo_seed.sql` — αφαιρέστε τους πριν από
την παραγωγική χρήση):

| Χρήστης | Ρόλος |
|---|---|
| `epivlepon@dimosrodou.demo` | Επιβλέπων (Βασίλειος Διακολιός) |
| `proistamenos@dimosrodou.demo` | Προϊστάμενος Δ.Υ. (Γεώργιος Παπαδόπουλος) |

Κοινός κωδικός: `Epivlepsi!2026`.

Δύο επιδεικτικά έργα: **ΔΡ-2026/014** (ασφαλτοστρώσεις Ιαλυσού, σε εκτέλεση) και
**ΔΡ-2025/007** (ενεργειακή αναβάθμιση σχολείου, σε περαίωση με εγκεκριμένη τελική
επιμέτρηση) — ώστε να δοκιμάζονται και τα έγγραφα του σταδίου περαίωσης.

---

## 2. Edge Function `generate-document`

```
POST /functions/v1/generate-document
Authorization: Bearer <το JWT του συνδεδεμένου χρήστη>

{
  "project_id": "…",                       // υποχρεωτικό
  "doc_code": "BEBAIOSI_PERATOSIS",        // υποχρεωτικό
  "entity_id": null,                       // επιμέτρηση / αφανείς / εγγύηση / ΑΠΕ
  "project_stage_id": null,                // σύνδεση με στάδιο του οδηγού
  "dry_run": true                          // προεπισκόπηση χωρίς αποθήκευση
}
```

### Ροή

1. **Ταυτότητα.** Ο πελάτης Supabase δημιουργείται με το JWT του καλούντος. Κάθε
   ανάγνωση και εγγραφή υπόκειται στα RLS policies — δεν υπάρχει service-role κλειδί
   στη συνάρτηση. Αν ο χρήστης δεν έχει πρόσβαση στο έργο, η κλήση αποτυγχάνει στο
   πρώτο ερώτημα.
2. **Πρότυπο.** Ανάγνωση από `document_templates` (η μεγαλύτερη ενεργή έκδοση) και των
   πεδίων του από `template_fields`.
3. **Δεδομένα.** Το `context.ts` συνθέτει το αντικείμενο του εγγράφου: φορέας, έργο,
   σύμβαση, ανάδοχος, επιβλέπων, προϊστάμενος, οικονομικά, και η οντότητα που
   συνοδεύει τον τύπο εγγράφου (αφανείς, επιμέτρηση με τις γραμμές της, εγγύηση,
   περαίωση, χρονοδιάγραμμα, ΑΠΕ, λογαριασμός).
4. **Απόδοση.** Το `render.ts` αντικαθιστά τα `{{…}}`. Σειρά πίνακα με
   `{{#each grammes}}` επαναλαμβάνεται ανά γραμμή επιμέτρησης. Για τα `.docx`
   ενοποιούνται πρώτα τα σπασμένα `<w:t>` κάθε παραγράφου — αλλιώς το Word σπάει τα
   placeholders σε πολλά runs και η αντικατάσταση αποτυγχάνει σιωπηρά.
5. **Πρωτόκολλο.** `next_protocol_no(org)` — ατομική πράξη ανά φορέα και έτος. Η
   συνάρτηση απορρίπτει αίτημα για φορέα διαφορετικό από τον δικό σας.
6. **Αποθήκευση.** Ανέβασμα στον κάδο `documents`, εγγραφή στον πίνακα `documents` με
   `payload`, SHA-256 `checksum` και `entity_type`/`entity_id`, και επιστροφή
   υπογεγραμμένου συνδέσμου μίας ώρας. Αν αποτύχει η καταχώριση, το αρχείο
   διαγράφεται — δεν μένουν ορφανά αντικείμενα στο storage.

Με `dry_run: true` δεν αποδίδεται πρωτόκολλο και δεν αποθηκεύεται τίποτε· επιστρέφεται
`preview_html`. Σε κάθε περίπτωση η απάντηση φέρει `missing_fields`: τα **υποχρεωτικά**
πεδία που έμειναν κενά και εμφανίζονται ως «—» στο έγγραφο.

### Ασφάλεια της χαρτογράφησης

Τα πρότυπα **δεν** περιέχουν SQL. Το `template_fields.source_path` είναι διαδρομή μέσα
στο αντικείμενο περιβάλλοντος (`contracts.signed_at`, `supervisor.full_name`,
`computed.maintenance_start`). Νέο πεδίο σε πρότυπο = μία γραμμή δεδομένων, χωρίς
ανάπτυξη κώδικα και χωρίς επιφάνεια επίθεσης.

---

## 3. Έτοιμα πρότυπα

| Κωδικός | Έγγραφο | Διάταξη |
|---|---|---|
| `APOFASI_ORISMOU` | Απόφαση ορισμού επιβλέποντος | άρθρο 136 §2 |
| `EGKRISI_XRONOD` | Απόφαση έγκρισης χρονοδιαγράμματος | άρθρο 145 §2 |
| `PPAE` | Έκθεση Παραλαβής Αφανών Εργασιών | άρθρο 151 §7 |
| `ANAL_EPIMETRISI` | Αναλυτική επιμέτρηση (με πίνακα γραμμών) | άρθρο 151 §2 |
| `BEBAIOSI_PERATOSIS` | Βεβαίωση Περάτωσης Εργασιών | άρθρο 168 §2 |
| `PRAXH_MEIWSHS_EGG` | Πράξη μείωσης εγγύησης κατά 70% | άρθρο 72 §14 περ. β΄ |

Κάθε πρότυπο ακολουθεί τη διοικητική δομή: επιστολόχαρτο ΕΛΛΗΝΙΚΗ ΔΗΜΟΚΡΑΤΙΑ, αριθμός
πρωτοκόλλου, ΘΕΜΑ, τίτλος, παραπομπή στη διάταξη, σκεπτικό «Έχοντας υπόψη», αποφασιστικό,
υπογραφές, ΚΟΙΝΟΠΟΙΗΣΗ. Έξοδος σε Α4 με σελιδοποίηση έτοιμη για εκτύπωση ή PDF.

Τα πρότυπα της Δ/νσης σε `.docx` ανεβαίνουν στον κάδο `templates` και δηλώνονται στο
`document_templates.storage_path`· τότε αγνοείται το ενσωματωμένο HTML.

Οι ετικέτες υπογραφής (`signatories[].label`) προσαρμόζονται από την Υπηρεσία στο γένος
και τον τίτλο του υπογράφοντος.

---

## 4. Σύνδεση με το front-end

- `api.generateDocument()` — ενιαία κλήση· στην παραγωγή καλεί την Edge Function, στη
  λειτουργία επίδειξης αποδίδει τοπικά (`src/lib/docgen.ts`) με τα ίδια πρότυπα.
- `<GenerateButton>` — εμφανίζεται μόνο όπου υπάρχει πρότυπο, στη σελίδα **Έγγραφα**
  (πίνακας υποχρεωτικών εγγράφων ανά στάδιο) και μέσα σε κάθε στάδιο του **Οδηγού**.
- Ο διάλογος δείχνει πρώτα **προεπισκόπηση** σε πλαίσιο Α4, με προειδοποίηση για τυχόν
  ελλιπή υποχρεωτικά πεδία (με την ελληνική ονομασία κάθε πεδίου). Ακολουθεί
  «Οριστική παραγωγή» (πρωτόκολλο + καταχώριση) ή «Εκτύπωση / PDF».

---

## 5. Θωράκιση ασφάλειας (migration 0016)

Ο γεννήτορας πολιτικών του `0012` παρέκαμπτε τους πίνακες χωρίς στήλη `project_id`.
Δέκα θυγατρικοί πίνακες έμεναν **χωρίς RLS** και ήταν αναγνώσιμοι μέσω PostgREST από
κάθε συνδεδεμένο χρήστη: `measurement_lines`, `ape_lines`, `payment_certificate_lines`,
`hidden_work_photos`, `measurement_audits`, `schedule_activities`, `schedule_periods`,
`new_price_items`, `project_stage_tasks`, `private_supervisors`.

Το `0016` ενεργοποιεί RLS και ορίζει πολιτικές που αντλούν το δικαίωμα από τον γονικό
πίνακα, με τους σωστούς περιορισμούς ανά ρόλο: ο ανάδοχος συντάσσει τις γραμμές
επιμέτρησης **μόνο όσο η επιμέτρηση είναι σε `draft`**, δεν βλέπει τον δειγματοληπτικό
έλεγχο (άρθρο 151 §3) ούτε τις εσωτερικές εργασίες του οδηγού. Καθηλώνεται επίσης το
`search_path` σε όλες τις `SECURITY DEFINER` συναρτήσεις και αφαιρείται κάθε δικαίωμα
από τον ρόλο `anon`.

Ο έλεγχος ασφαλείας του Supabase δεν επιστρέφει πλέον κανένα σφάλμα. Παραμένουν τρεις
προειδοποιήσεις για τις τρεις `SECURITY DEFINER` συναρτήσεις (`stage_blockers`,
`instantiate_workflow`, `next_protocol_no`) — είναι σκόπιμο: καθεμία ελέγχει εσωτερικά
τα δικαιώματα του καλούντος. Στο **Authentication → Policies** αξίζει να ενεργοποιηθεί
ο έλεγχος διαρρευσάντων κωδικών (HaveIBeenPwned).

---

## 6. Δοκιμές

```bash
psql -d epivlepsi -f tests/supabase_stubs.sql
psql -d epivlepsi -f schema_full.sql
psql -d epivlepsi -f tests/test_failsafe.sql   # 12 σενάρια fail-safe
node tests/fnharness/harness.mjs               # απόδοση των 6 προτύπων
```

Το `tests/fnharness` τρέχει τον **πραγματικό** κώδικα της Edge Function πάνω σε
στιγμιότυπα της βάσης, με ψεύτικο πελάτη Supabase, και ελέγχει για κάθε πρότυπο:
αναντικατάστατα `{{…}}`, κενά υποχρεωτικά πεδία, `undefined`, `[object Object]`, `NaN`.
Ελέγχονται δύο σενάρια — έργο σε εκτέλεση και έργο σε περαίωση.

---

## 7. Εκκρεμότητες

1. Ανέβασμα των πραγματικών `.docx` της Δ/νσης στον κάδο `templates`.
2. Ανέβασμα φωτογραφιών ΠΠΑΕ στον κάδο `photos` από τη σελίδα αφανών εργασιών.
3. Νυχτερινή Edge Function: σιωπηρές εγκρίσεις και ρήτρες ημερολογίου.
4. Φόρμες δημιουργίας ΑΠΕ και λογαριασμών.
5. Ειδοποιήσεις από το `v_deadline_watch`.
6. Συμπλήρωση του `work_groups` κατά την Υ.Α. ΔΝΣγ/οικ.38107/ΦΝ 466/2017.
7. Διαγραφή της αδρανούς συνάρτησης `bootstrap-sql` από το Dashboard.
