# Απόλυτος Οδηγός & Σύστημα Ελέγχου Επίβλεψης Έργων

Web app που καθοδηγεί τον **Επιβλέποντα Μηχανικό** δημοσίων έργων βήμα προς βήμα, από
την υπογραφή της σύμβασης μέχρι τη Βεβαίωση Περάτωσης, κατά τον **ν. 4412/2016**
(Βιβλίο Ι, Μέρος Β΄) όπως ισχύει μετά τον **ν. 4782/2021**.

Χρήστης/συντάκτης: ΠΕ Μηχανολόγος Μηχανικός, Δήμος Ρόδου. **Απαντάμε στα ελληνικά.**

## Στοίβα & τοποθεσίες

| | |
|---|---|
| Βάση | Supabase (PostgreSQL 17, RLS, Edge Functions) — project `nubxaclmwuhqkvuvmvos` |
| Front-end | React 18 / Vite 5 / TypeScript / Tailwind, στον φάκελο `app/` |
| Δημοσίευση | GitHub Pages μέσω Actions (`.github/workflows/deploy.yml`, μόνο σε push στο `main`) |
| Repo | `vasilis1730-web/epivlepon` → https://vasilis1730-web.github.io/epivlepon/ |

## Τι είναι έτοιμο

- **Σχήμα 66 πινάκων** (σχήμα `public`· επιπλέον 2 εσωτερικοί στο σχήμα `app`),
  **μηχανή ροής 36 σταδίων** με fail-safe triggers, **78 νομικές παραπομπές**,
  RLS με διαχωρισμό ρόλων.
- **Front-end**: πίνακας έργων, οδηγός σταδίων, ημερολόγιο, αφανείς εργασίες,
  επιμετρήσεις, λογαριασμοί, ΑΠΕ, εγγυήσεις, περαίωση, έγγραφα, οθόνη «Νέο έργο».
- **Παραγωγή εγγράφων** από 6 πρότυπα (Edge Function `generate-document` + τοπική απόδοση).
- **Νυχτερινή εργασία προθεσμιών**: `app.run_nightly_jobs`, pg_cron στις 02:15 UTC.

### Δοκιμές

| Αρχείο | Τι καλύπτει |
|---|---|
| `tests/test_failsafe.sql` | 12 σενάρια — οι triggers απορρίπτουν παράνομες μεταβάσεις |
| `tests/test_create_project.sql` | 8 σενάρια — έλεγχοι εγκυρότητας, οικονομικά, προθεσμίες, RLS |
| `tests/fnharness/harness.mjs` | 6 πρότυπα εγγράφων, χωρίς δίκτυο |

Οι δοκιμές SQL τρέχουν σε **καθαρή τοπική PostgreSQL**: πρώτα `tests/supabase_stubs.sql`,
μετά όλα τα `supabase/migrations/*.sql` **με τη σειρά**. Καθαρή εγκατάσταση = 0 σφάλματα.

```bash
# SQL
psql -v ON_ERROR_STOP=1 -f tests/supabase_stubs.sql
for f in supabase/migrations/*.sql; do psql -v ON_ERROR_STOP=1 -f "$f"; done
psql -f tests/test_failsafe.sql
psql -f tests/test_create_project.sql

# Πρότυπα εγγράφων (χρειάζεται esbuild + jszip)
node tests/fnharness/harness.mjs

# Έλεγχος τύπων & build
cd app && npm ci && npm run build     # build = tsc --noEmit && vite build
```

> Στο `test_failsafe.sql` τα μηνύματα `ERROR:` είναι **το αναμενόμενο αποτέλεσμα** —
> κάθε τέτοια δοκιμή είναι αρνητικός ισχυρισμός: ο trigger *πρέπει* να απορρίψει την
> ενέργεια. Μην τα εκλάβεις ως αποτυχία.

> Το `tests/fnharness/supabase-stub.mjs` το **παράγει το ίδιο το harness** κατά την
> εκτέλεση — γι' αυτό είναι στο `.gitignore` και δεν παρακολουθείται.

## Λογαριασμοί εφαρμογής

| Λογαριασμός | Ρόλος |
|---|---|
| `vasilis1730@gmail.com` | admin — ανοίγει έργα, ορίζει ρόλους |
| `epivlepon@dimosrodou.demo` | κανένας υπηρεσιακός ρόλος |
| `proistamenos@dimosrodou.demo` | `proistamenos_dy` |

Οι demo λογαριασμοί έχουν κωδικό `Epivlepsi!2026`. Ο κωδικός του διαχειριστή είναι
προσωπικός· αλλάζει από το Supabase → Authentication → Users.

**Ο διαχωρισμός είναι σκόπιμος: ο επιβλέπων ΔΕΝ ανοίγει έργα.** Το έργο το ανοίγει η
Διευθύνουσα Υπηρεσία, που ορίζει και τον επιβλέποντα (**άρθρο 136 §2**). Επιβάλλεται σε
**τρία επίπεδα** — RLS policy, έλεγχος μέσα στην `create_project_full()`, και απόκρυψη
του κουμπιού στο UI. Αν αλλάξεις το ένα, έλεγξε και τα άλλα δύο.

## Παγίδες που έχουμε ήδη πληρώσει

**«Database error querying schema» στη σύνδεση** — δύο ανεξάρτητες αιτίες, και οι δύο
διορθωμένες:
- (α) ο ρόλος `supabase_auth_admin` χρειάζεται `USAGE` στο σχήμα `app` και `EXECUTE`
  στην `app.handle_new_user()`, αλλιώς αποτυγχάνει **κάθε** ενέργεια στον `auth.users`·
- (β) οι στήλες `confirmation_token`, `recovery_token`, `email_change_token_new`,
  `email_change` είναι `text` χωρίς προεπιλογή και το GoTrue τις διαβάζει σε **μη
  μηδενίσιμα** πεδία Go — ένα `NULL` εκεί ρίχνει κάθε σύνδεση. Όποιος εισάγει χρήστη
  με απευθείας `INSERT` **πρέπει** να τις θέτει σε `''`.

**Πλασματικές προθεσμίες** — η `app.compute_stage_due()` έπεφτε σιωπηλά στην ημερομηνία
υπογραφής για κάθε άγνωστη αφετηρία, δείχνοντας «Βεβαίωση Περάτωσης, εκπρόθεσμο 145
ημέρες» σε έργο που δεν είχε ξεκινήσει. Διορθώθηκε στο **0023**, μαζί με το πεδίο
`deadline_basis` σε κάθε στάδιο του `app/src/data/workflow.json`.
**Κανόνας: ποτέ ημερομηνία χωρίς αφετηρία — `NULL` είναι η σωστή απάντηση, όχι μια εικασία.**

**Ελληνικά κεφαλαία** — χάνουν τους τόνους, αλλά το `text-transform: uppercase` δεν το
κάνει, ούτε με `lang="el"`. Γίνεται στον κώδικα: `grUpper` (NFD normalize + αφαίρεση
U+0300/0301/0342/0384).

**Ρυθμίσεις build** — το `vite build` διαβάζει το `app/.env.production`. Για δοκιμή σε
λειτουργία επίδειξης χρειάζεται `VITE_DEMO=1`.

**Κλειδιά** — οι ρυθμίσεις Supabase περνούν ως `env:` στο `.github/workflows/deploy.yml`.
Το ανώνυμο κλειδί είναι **δημόσιο εξ ορισμού**· η προστασία ασκείται από τα RLS policies.
Το **service-role κλειδί δεν χρησιμοποιείται πουθενά — μην το βάλεις.**

**Αρίθμηση migrations** — η αρίθμηση στο Supabase **δεν ταυτίζεται** με τα τοπικά ονόματα:
μερικά εφαρμόστηκαν ως `0022_auth_admin_grants` και `0023_fix_auth_user_null_tokens`, και
το περιεχόμενό τους ενσωματώθηκε **αναδρομικά** στα τοπικά `0015` και `0020`. Ο φάκελος
`supabase/migrations/` είναι η **καθαρή σειρά για νέα εγκατάσταση**, όχι το ιστορικό της
ζωντανής βάσης.

**35 ή 36 στάδια;** — Και τα δύο σωστά. Το `S01B_IFE` (Ιδιωτικός Φορέας Επίβλεψης)
δημιουργείται υπό συνθήκη, μέσω `applies_when` / `app.stage_applies()`. Έργο χωρίς Ι.Φ.Ε.
παίρνει 35 στάδια — δεν είναι σφάλμα.

## Πώς δουλεύουμε

- **Νομική ακρίβεια πάνω από ευκολία**: κάθε κανόνας φέρει τη διάταξή του.
- **Σιωπηρές εγκρίσεις** υλοποιούνται **μόνο** εκεί που τις ορίζει ο νόμος
  (145 §2, 147 §5, 151 §7, 152) — **όχι στις επιμετρήσεις**.
- **Ποινικές ρήτρες**: καταχωρίζονται ως **υπολογισμός**, ποτέ ως **επιβολή** — η επιβολή
  είναι πράξη της Διευθύνουσας Υπηρεσίας.
- **Πριν πεις ότι κάτι δουλεύει, τρέξε το.**

## Τι εκκρεμεί, κατά σειρά

1. Φόρμα καταχώρισης **ΑΠΕ και λογαριασμού** (υπάρχουν μόνο οι οθόνες προβολής).
2. **Ανέβασμα φωτογραφιών ΠΠΑΕ** — ο κάδος `photos` υπάρχει, λείπει η σύνδεση με τη
   σελίδα αφανών εργασιών (**άρθρο 151 §7**: η φωτογραφική τεκμηρίωση είναι υποχρεωτική).
3. Φόρτωση των **πραγματικών προτύπων `.docx`** της Δ/νσης στον κάδο `templates`.
4. Συμπλήρωση του **`work_groups`** κατά την Υ.Α. ΔΝΣγ/οικ.38107/ΦΝ 466/2017.
5. **Ειδοποιήσεις** από την όψη `v_deadline_watch`.
6. **Απενεργοποίηση δημόσιων εγγραφών** στο Supabase πριν μοιραστεί ο σύνδεσμος.
