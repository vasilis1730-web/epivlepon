# Απόλυτος Οδηγός & Σύστημα Ελέγχου Επίβλεψης Έργων

Web app που καθοδηγεί τον **Επιβλέποντα Μηχανικό** δημοσίων έργων βήμα προς βήμα, από
την υπογραφή της σύμβασης μέχρι τη Βεβαίωση Περάτωσης, κατά τον **ν. 4412/2016**
(Βιβλίο Ι, Μέρος Β΄) όπως ισχύει μετά τον **ν. 4782/2021**.

Χρήστης/συντάκτης: ΠΕ Μηχανολόγος Μηχανικός, Δήμος Ρόδου. **Απαντάμε στα ελληνικά.**

## Στοίβα & τοποθεσίες

| | |
|---|---|
| Βάση | Supabase (PostgreSQL 17, RLS, Edge Functions) — project `nubxaclmwuhqkvuvmvos` |
| Front-end | React 18 / Vite 5 / TypeScript / Tailwind, στον φάκελο `app/` |
| Δημοσίευση | GitHub Pages μέσω Actions (`.github/workflows/deploy.yml`, μόνο σε push στο `main`) |
| Repo | `vasilis1730-web/epivlepon` → https://vasilis1730-web.github.io/epivlepon/ |

## Τι είναι έτοιμο

- **Σχήμα 66 πινάκων** (σχήμα `public`· επιπλέον 2 εσωτερικοί στο σχήμα `app`),
  **μηχανή ροής 36 σταδίων** με fail-safe triggers, **78 νομικές παραπομπές**,
  RLS με διαχωρισμό ρόλων.
- **Front-end**: πίνακας έργων, οδηγός σταδίων, ημερολόγιο, αφανείς εργασίες,
  επιμετρήσεις, λογαριασμοί, ΑΠΕ, εγγυήσεις, περαίωση, έγγραφα, οθόνη «Νέο έργο».
- **Φόρμες καταχώρισης** προϋπολογισμού μελέτης, ΑΠΕ και λογαριασμού
  (`Budget.tsx`, `NewApe.tsx`, `NewPayment.tsx`).
- **Φωτογραφική τεκμηρίωση ΠΠΑΕ**: ανέβασμα αρχείων στον κάδο `photos` (άρθρο 151 §7).
- **Παραγωγή εγγράφων** από 8 πρότυπα (Edge Function `generate-document` + τοπική απόδοση),
  **συν τρία υπολογιζόμενα** του ΑΠΕ: αιτιολογική έκθεση, φύλλο ελέγχου επί έλαττον και
  εισήγηση προς τη Δημοτική Επιτροπή (`apeDocuments.ts`, route `/erga/:id/ape/:apeId/eggrafa`).
- **Νυχτερινή εργασία προθεσμιών**: `app.run_nightly_jobs`, pg_cron στις 02:15 UTC.
- **Ειδοποιήσεις** (`/eidopoiiseis`): ό,τι εκκρεμεί σε όλα τα έργα, με καμπάνα στην κεφαλίδα.

### Δοκιμές

| Αρχείο | Τι καλύπτει |
|---|---|
| `tests/test_failsafe.sql` | 13 σενάρια — triggers, όρια ΑΠΕ και RLS |
| `tests/test_create_project.sql` | 8 σενάρια — έλεγχοι εγκυρότητας, οικονομικά, προθεσμίες, RLS |
| `tests/fnharness/harness.mjs` | 6 πρότυπα εγγράφων, χωρίς δίκτυο |
| `tests/apetotals/harness.mjs` | τα σύνολα ΑΠΕ έναντι **πραγματικών εγκεκριμένων πινάκων** |

Οι δοκιμές SQL τρέχουν σε **καθαρή τοπική PostgreSQL**: πρώτα `tests/supabase_stubs.sql`,
μετά όλα τα `supabase/migrations/*.sql` **με τη σειρά**. Καθαρή εγκατάσταση = 0 σφάλματα.

```bash
# SQL
psql -v ON_ERROR_STOP=1 -f tests/supabase_stubs.sql
for f in supabase/migrations/*.sql; do psql -v ON_ERROR_STOP=1 -f "$f"; done
psql -f tests/test_failsafe.sql
psql -f tests/test_create_project.sql

# Πρότυπα εγγράφων (χρειάζεται esbuild + jszip)
node tests/fnharness/harness.mjs

# Σύνολα ΑΠΕ έναντι των εγκεκριμένων πινάκων της Δ/νσης (χρειάζεται esbuild)
node tests/apetotals/harness.mjs

# Έλεγχος τύπων & build
cd app && npm ci && npm run build     # build = tsc --noEmit && vite build
```

> Στο `test_failsafe.sql` τα μηνύματα `ERROR:` είναι **το αναμενόμενο αποτέλεσμα** —
> κάθε τέτοια δοκιμή είναι αρνητικός ισχυρισμός: ο trigger *πρέπει* να απορρίψει την
> ενέργεια. Μην τα εκλάβεις ως αποτυχία.

> Το `tests/fnharness/supabase-stub.mjs` το **παράγει το ίδιο το harness** κατά την
> εκτέλεση — γι' αυτό είναι στο `.gitignore` και δεν παρακολουθείται.

## Λογαριασμοί εφαρμογής

| Λογαριασμός | Ρόλος |
|---|---|
| `vasilis1730@gmail.com` | admin — ανοίγει έργα, ορίζει ρόλους |
| `epivlepon@dimosrodou.demo` | κανένας υπηρεσιακός ρόλος |
| `proistamenos@dimosrodou.demo` | `proistamenos_dy` |

Οι demo λογαριασμοί έχουν κωδικό `Epivlepsi!2026`. Ο κωδικός του διαχειριστή είναι
προσωπικός· αλλάζει από το Supabase → Authentication → Users.

**Ο διαχωρισμός είναι σκόπιμος: ο επιβλέπων ΔΕΝ ανοίγει έργα.** Το έργο το ανοίγει η
Διευθύνουσα Υπηρεσία, που ορίζει και τον επιβλέποντα (**άρθρο 136 §2**). Επιβάλλεται σε
**τρία επίπεδα** — RLS policy, έλεγχος μέσα στην `create_project_full()`, και απόκρυψη
του κουμπιού στο UI. Αν αλλάξεις το ένα, έλεγξε και τα άλλα δύο.

## Παγίδες που έχουμε ήδη πληρώσει

**«Database error querying schema» στη σύνδεση** — δύο ανεξάρτητες αιτίες, και οι δύο
διορθωμένες:
- (α) ο ρόλος `supabase_auth_admin` χρειάζεται `USAGE` στο σχήμα `app` και `EXECUTE`
  στην `app.handle_new_user()`, αλλιώς αποτυγχάνει **κάθε** ενέργεια στον `auth.users`·
- (β) οι στήλες `confirmation_token`, `recovery_token`, `email_change_token_new`,
  `email_change` είναι `text` χωρίς προεπιλογή και το GoTrue τις διαβάζει σε **μη
  μηδενίσιμα** πεδία Go — ένα `NULL` εκεί ρίχνει κάθε σύνδεση. Όποιος εισάγει χρήστη
  με απευθείας `INSERT` **πρέπει** να τις θέτει σε `''`.

**Πλασματικές προθεσμίες** — η `app.compute_stage_due()` έπεφτε σιωπηλά στην ημερομηνία
υπογραφής για κάθε άγνωστη αφετηρία, δείχνοντας «Βεβαίωση Περάτωσης, εκπρόθεσμο 145
ημέρες» σε έργο που δεν είχε ξεκινήσει. Διορθώθηκε στο **0023**, μαζί με το πεδίο
`deadline_basis` σε κάθε στάδιο του `app/src/data/workflow.json`.
**Κανόνας: ποτέ ημερομηνία χωρίς αφετηρία — `NULL` είναι η σωστή απάντηση, όχι μια εικασία.**

**Ελληνικά κεφαλαία** — χάνουν τους τόνους, αλλά το `text-transform: uppercase` δεν το
κάνει, ούτε με `lang="el"`. Γίνεται στον κώδικα: `grUpper` (NFD normalize + αφαίρεση
U+0300/0301/0342/0384).

**Ρυθμίσεις build** — το `vite build` διαβάζει το `app/.env.production`. Για δοκιμή σε
λειτουργία επίδειξης χρειάζεται `VITE_DEMO=1`.

**Κλειδιά** — οι ρυθμίσεις Supabase περνούν ως `env:` στο `.github/workflows/deploy.yml`.
Το ανώνυμο κλειδί είναι **δημόσιο εξ ορισμού**· η προστασία ασκείται από τα RLS policies.
Το **service-role κλειδί δεν χρησιμοποιείται πουθενά — μην το βάλεις.**

**Αρίθμηση migrations** — η αρίθμηση στο Supabase **δεν ταυτίζεται** με τα τοπικά ονόματα:
μερικά εφαρμόστηκαν ως `0022_auth_admin_grants` και `0023_fix_auth_user_null_tokens`, και
το περιεχόμενό τους ενσωματώθηκε **αναδρομικά** στα τοπικά `0015` και `0020`. Ο φάκελος
`supabase/migrations/` είναι η **καθαρή σειρά για νέα εγκατάσταση**, όχι το ιστορικό της
ζωντανής βάσης.

**35 ή 36 στάδια;** — Και τα δύο σωστά. Το `S01B_IFE` (Ιδιωτικός Φορέας Επίβλεψης)
δημιουργείται υπό συνθήκη, μέσω `applies_when` / `app.stage_applies()`. Έργο χωρίς Ι.Φ.Ε.
παίρνει 35 στάδια — δεν είναι σφάλμα.

## Πώς δουλεύουμε

- **Νομική ακρίβεια πάνω από ευκολία**: κάθε κανόνας φέρει τη διάταξή του.
- **Σιωπηρές εγκρίσεις** υλοποιούνται **μόνο** εκεί που τις ορίζει ο νόμος
  (145 §2, 147 §5, 151 §7, 152) — **όχι στις επιμετρήσεις**.
- **Ποινικές ρήτρες**: καταχωρίζονται ως **υπολογισμός**, ποτέ ως **επιβολή** — η επιβολή
  είναι πράξη της Διευθύνουσας Υπηρεσίας.
- **Πριν πεις ότι κάτι δουλεύει, τρέξε το.**

## Η αλυσίδα προϋπολογισμός → ΑΠΕ → λογαριασμός

Ο ΑΠΕ **δεν** γράφεται από το μηδέν. Η σειρά είναι:

1. **Προϋπολογισμός μελέτης** (`/erga/:id/proypologismos`) — έκδοση 0, τα συμβατικά
   μεγέθη. Χωρίς αυτόν η φόρμα ΑΠΕ αρνείται να ξεκινήσει: ΑΠΕ χωρίς αφετηρία
   σύγκρισης δεν τεκμηριώνει τίποτε.
2. **1ος ΑΠΕ** — προσυμπληρώνεται από τον προϋπολογισμό.
3. **2ος και επόμενοι** — προσυμπληρώνονται από τον τελευταίο **εγκεκριμένο** ΑΠΕ:
   οι ποσότητές του γίνονται `qty_previous`.

Στον ΑΠΕ μεταβάλλονται **μόνο ποσότητες**· οι τιμές μονάδος είναι συμβατικές. Νέα
άρθρα εισάγονται ρητά (`is_new_item`) και απαιτούν Π.Κ.Τ.Μ.Ν.Ε. (**156 §5**).

**Μοντέλο συνόλων** (`apeTotals` στο `rules.ts`) — αντιγράφει την επίσημη δομή των
πινάκων της Δ/νσης:

```
  Άθροισμα Εργασιών
+ Γ.Ε. & Ο.Ε. (ενιαίο %, ΕΠΑΝΥΠΟΛΟΓΙΖΕΤΑΙ στο νέο άθροισμα)
= ΣΥΝΟΛΟ 1
+ Απρόβλεπτα (το ΥΠΟΛΟΙΠΟ μετά την απορρόφηση)
= ΣΥΝΟΛΟ 2   <- μένει ΙΣΟ με της συμβασης
```

**Ο ΑΠΕ είναι «σε ισοζύγιο»**: η αύξηση των εργασιών απορροφάται από τα απρόβλεπτα,
που συρρικνώνονται ισόποσα. Στην Κοσκινού τα απρόβλεπτα πήγαν 32.560,47 -> 0,65 και το
ΣΥΝΟΛΟ 2 έμεινε 249.630,30. Αν η αύξηση εξαντλήσει τα απρόβλεπτα, η διαφορά **δεν**
βαφτίζεται σιωπηρά αύξηση: περνά από το **άρθρο 132** ή από συμπληρωματική σύμβαση.
Το `balanced` είναι `false` όταν υπάρχει υπέρβαση — αλλιώς το ΣΥΝΟΛΟ 2 «κλείνει»
αριθμητικά προσθέτοντας αρνητικό υπόλοιπο και κρύβει το πρόβλημα.

Οι **εργασίες του άρθρου 132** τηρούνται σε ΞΕΧΩΡΙΣΤΗ ενότητα και στήλη: δεν
καλύπτονται από απρόβλεπτα, προσαυξάνουν τη σύμβαση, με όριο **15%** (132 §2).

**Πεδία που απαιτούν τα έγγραφα** (migration **0024**): `contracts.award_body` /
`award_decision_no` / `award_decision_date` και `legalization_doc_no` /
`legalization_doc_date` / `legalization_authority` — συνθέτουν το «Α. ΙΣΤΟΡΙΚΟ» κάθε
αιτιολογικής έκθεσης· `ape_lines.revision_code` (Κωδικός Αναθεώρησης: ΟΙΚ 2113,
ΥΔΡ 6068 — ο `budget_items` τον είχε ήδη, ο `ape_lines` όχι, οπότε χανόταν στη
μετάβαση)· `ape.accounting_error` (λογιστικό λάθος) και `ape.article_132_value`.
Το `item_code` είναι το **Α.Τ.**, όχι κωδικός τιμολογίου ΝΕΤ.

**Ο προϋπολογισμός πρέπει να συμφωνεί με τη σύμβαση.** Η οθόνη προϋπολογισμού
αντιπαραβάλλει εργασίες + ΓΕ & ΟΕ με το ΣΥΝΟΛΟ 1 της σύμβασης και αρνείται την
καταχώριση σε απόκλιση — αλλιώς κάθε ποσό του ΑΠΕ βγαίνει πλασματικό.

**Τα όρια του άρθρου 156, όπως τα ελέγχει το επίσημο φύλλο:**
- **20% ανά ομάδα** ισχύει **και στα επί πλέον**, όχι μόνο στα επί έλαττον. Η υπέρβαση
  δεν απαγορεύεται αυτοτελώς: πρέπει να **καλύπτεται από τα χρησιμοποιηθέντα
  απρόβλεπτα**, αλλιώς είναι παρέκκλιση.
- **10% επί έλαττον** μετράται στο **ΣΥΝΟΛΟ 1** (εργασίες + ΓΕ & ΟΕ), **όχι** στη
  συνολική συμβατική αξία που περιλαμβάνει τα απρόβλεπτα.
- **Τεχνικό Συμβούλιο** απαιτείται σε χρήση επί έλαττον, σε εργασίες άρθρου 132, ή σε
  συμπληρωματική σύμβαση.

Πηγή αλήθειας: πέντε πραγματικά έργα (Έμπωνα, Καλλιθέα, Κοσκινού, Ν. Ρόδου,
Πυρκαγιές). Το `tests/apetotals/harness.mjs` κλειδώνει τα σύνολα σε αυτά — **54
ισχυρισμοί**, συμπεριλαμβανομένου του φύλλου ελέγχου στήλη προς στήλη.

## Έγγραφα του ΑΠΕ

Τα τρία έγγραφα **δεν είναι έντυπα συμπλήρωσης κενών**: υπολογίζονται εξ ολοκλήρου από
τον ΑΠΕ και τη σύμβαση (`app/src/lib/apeDocuments.ts`), με τον ίδιο περιτυλιγμό
`wrapDocument` ώστε η εκτύπωση να μένει ενιαία. Αν αλλάξει ο ΑΠΕ, αλλάζουν και τα τρία.

Στο **φύλλο ελέγχου** η αρίθμηση των στηλών ([3]–[15]) αντιγράφει αυτούσια το έντυπο
της Δ/νσης, ώστε να αντιπαραβάλλεται γραμμή προς γραμμή. Δύο λεπτά σημεία:
- Το `[1.4]` («παράβαση του 20%») αφορά **μόνο τα επί έλαττον** — η υπέρβαση των επί
  πλέον ελέγχεται στον Πίνακα 2.
- Τα «χρησιμοποιηθέντα απρόβλεπτα» του Πίνακα 2 δεν μπορούν να ξεπερνούν το κονδύλιο·
  ό,τι απαιτείται πέραν αυτού είναι υπέρβαση της σύμβασης, όχι χρήση απροβλέπτων.

**Ελληνική γραμματική**: τα τακτικά αριθμητικά κλίνονται (`ord.nom/gen/acc` —
«ο 1ος», «του 1ου», «τον 1ο»). Η γενική του φορέα αποφεύγεται συντακτικά, γιατί
«του Δήμος Ρόδου» είναι λάθος και η κλίση ονομάτων δεν αυτοματοποιείται αξιόπιστα.

Ο λογαριασμός είναι **σωρευτικός**: συμπληρώνονται τα σύνολα από την αρχή του έργου
και το `paymentTotals` αφαιρεί τα ήδη πιστοποιημένα. Το ποσό περιόδου, οι κρατήσεις
και το πληρωτέο δεν πληκτρολογούνται ποτέ.

**Παγίδα που διορθώθηκε**: το `ape_lines.work_group_id` είναι FK, ενώ ο τύπος
`ApeLine` φέρει `work_group` ως κείμενο. Το `getApes` έκανε `select('*, ape_lines(*)')`
χωρίς join, οπότε στην παραγωγή το `work_group` ήταν `undefined` — και ο έλεγχος του
ορίου **20% ανά ομάδα (156 §3γ)** ομαδοποιούσε τα πάντα σε μία ομάδα, δηλαδή δεν
έλεγχε τίποτε. Το front-end μεταφέρει πλέον τον **τίτλο** της ομάδας παντού· η
`workGroupIds` δέχεται και κωδικό και τίτλο.

**Το `photos_count` δεν το γράφει η εφαρμογή.** Το συντηρεί ο trigger
`app.sync_hidden_photos_count` από τις πραγματικές εγγραφές του `hidden_work_photos`,
και το constraint `hwn_photos_chk` απαγορεύει καταχώριση ελέγχου χωρίς φωτογραφία.
Παλαιότερα η `inspectHiddenWork` έστελνε τον αριθμό ως πεδίο, με τον χρήστη να τον
**πληκτρολογεί** — δηλαδή μπορούσες να δηλώσεις τεκμηρίωση που δεν υπήρχε. Ο κάδος
`photos` είναι ιδιωτικός· η προβολή γίνεται με υπογεγραμμένους συνδέσμους μιας ώρας.

## Ομάδες εργασιών — τις ορίζει η μελέτη, όχι ένας καθολικός κατάλογος

Στους εγκεκριμένους πίνακες της Δ/νσης οι ομάδες **αριθμούνται 1, 2, 3 …** και ο τίτλος
τους είναι του συγκεκριμένου έργου: «1 ΧΩΜΑΤΟΥΡΓΙΚΑ / 2 ΣΚΥΡΟΔΕΜΑΤΑ» (Κοσκινού),
«1 ΚΑΘΑΙΡΕΣΕΙΣ ΣΚΥΡΟΔΕΜΑΤΑ / 2 ΣΚΥΡΟΔΕΜΑΤΑ ΟΠΛΙΣΜΟΙ» (Καλλιθέα),
«1 ΔΑΣΟΤΕΧΝΙΚΑ / 2 ΕΡΓΑ ΑΠΟΚΑΤΑΣΤΑΣΕΩΝ ΚΑΙ ΠΡΟΣΤΑΣΙΑΣ» (Ν. Ρόδου, Πυρκαγιές).
Γι' αυτό το `work_groups` έχει πλέον `project_id` (**migration 0025**): με `NULL` είναι
ο **ενδεικτικός** κατάλογος της κατηγορίας, συμπληρωμένο είναι ομάδα του έργου. Η οθόνη
προϋπολογισμού ξεκινά από την πρόταση της κατηγορίας και ο μηχανικός τη διορθώνει ώστε
να αποδίδει τη μελέτη· ό,τι δηλωθεί καταγράφεται ως ομάδα του έργου.

**Το παράρτημα της Υ.Α. ΔΝΣγ/οικ.38107/ΦΝ 466/2017 ΔΕΝ έχει αντιγραφεί αυτούσιο.** Η
απόφαση ταυτοποιήθηκε (ΦΕΚ Β΄1956/07.06.2017, ισχύς από 15.07.2017 μετά την
ΔΝΣγ/οικ.42218/ΦΝ 466), αλλά το κείμενό της δεν ήταν προσβάσιμο από αυτό το περιβάλλον
— κάθε πηγή (e-nomothesia, taxheaven, kodiko, et.gr) μπλοκάρεται από τον proxy. Ο
κατάλογος στο `0013` είναι **πρόταση εκκίνησης** και δηλώνεται ως τέτοια στον σχολιασμό
του migration· δεν παριστάνει το παράρτημα. Όποιος έχει το ΦΕΚ, αντικαθιστά το μπλοκ
του `0013` και το `workGroups` του `app/src/data/workflow.json` — τίποτε άλλο δεν αλλάζει.

**Γραμμή χωρίς ομάδα ήταν ανέλεγκτη γραμμή.** Ο έλεγχος του 20% (156 §3γ) γίνεται με
join στις ομάδες: ό,τι είχε `work_group_id is null` έμενε **εκτός αθροίσματος** και
περνούσε αθόρυβα. Το 0025 προσθέτει hard blocker `APE_LINE_NO_GROUP`, και το
`workGroupIds` υιοθετεί ως ομάδα του έργου κάθε τίτλο που δεν αντιστοιχίζεται — το
`null` δεν είναι αποδεκτή έκβαση.

## Ειδοποιήσεις

Το `app/src/lib/alerts.ts` **παράγει** τις ειδοποιήσεις από την κατάσταση των έργων —
δεν τις αποθηκεύει. Δεν υπάρχει «διαβασμένο» και δεν υπάρχει «απόρριψη»: μια
εκπρόθεσμη προθεσμία δεν παύει να είναι εκπρόθεσμη επειδή την είδε κάποιος, και η μόνη
διαγραφή είναι η τακτοποίηση της αιτίας. Έτσι δεν χρειάζεται ούτε πίνακας ούτε
συγχρονισμός: ο κώδικας είναι κοινός για demo και παραγωγή, γιατί χτίζεται πάνω στις
ίδιες `api.*` κλήσεις.

Σειρά βαρύτητας — **πρώτες οι σιωπηρές εγκρίσεις**, γιατί εκεί η αδράνεια δεν
αναβάλλει την απόφαση αλλά την παίρνει (145 §2, 147 §5, 151 §7, 152). Ακολουθούν
παραβάσεις ορίων ΑΠΕ (156), αφανείς εργασίες (151 §7), προθεσμίες σταδίων και η
συνολική προθεσμία του έργου (147 §8).

Ο μετρητής της καμπάνας δείχνει **μόνο εκπρόθεσμα και κρίσιμα (≤5 ημέρες)**: ένας
αριθμός που ανάβει για κάθε προθεσμία δεκαπενθημέρου παύει γρήγορα να διαβάζεται.

## Ασφάλεια: δύο πράγματα που βρέθηκαν επειδή τα τρέξαμε

**Αμοιβαία αναδρομή στα RLS των εγγράφων** (διορθώθηκε στο **0026**). Το
`documents_sel` διάβαζε `document_communications`, και το
`document_communications_sel` διάβαζε `documents`. Η PostgreSQL εντοπίζει τον κύκλο
στον σχεδιασμό και **ματαιώνει ολόκληρο το ερώτημα** — δεν «κόβει» τον έναν κλάδο του
OR. Αποτέλεσμα: `select` στα `documents`, `document_communications` και
`document_signatures` απέτυχε με «infinite recursion detected in policy» για **κάθε**
χρήστη, επιβλέποντα και προϊστάμενο συμπεριλαμβανομένων. Η οθόνη «Έγγραφα» ήταν
άχρηστη στην παραγωγή· δεν φάνηκε ποτέ επειδή η ανάπτυξη γινόταν σε λειτουργία
επίδειξης. Ο ένας κρίκος έγινε συνάρτηση `security definer` (`app.doc_project`,
`app.doc_communicated_to`), που δεν ξαναπερνά από το RLS του άλλου πίνακα.
**Κανόνας: δύο policies δεν διαβάζουν η μία τον πίνακα της άλλης.**

**Η εγγραφή έδινε αυτόματα προφίλ στον φορέα** (διορθώθηκε στο **0027**). Χρήστης
χωρίς κανέναν υπηρεσιακό ρόλο έβλεπε 0 έργα και 0 έγγραφα — αλλά **3 προφίλ και 1
φορέα**: ονόματα, ειδικότητες και στοιχεία του προσωπικού της Δ/νσης. Πλέον προφίλ
αποκτά μόνο διεύθυνση που ταιριάζει στο `app.signup_allowlist` (ακριβείς διευθύνσεις
των υπαρχόντων προφίλ + ο τομέας του φορέα από το `organizations.email`). Χωρίς προφίλ
η `app.my_org()` επιστρέφει `NULL` και καμία policy δεν επιστρέφει γραμμή. Η ένταξη
λογαριασμού που εγγράφηκε εκτός λίστας γίνεται ρητά, με `public.admit_user()`, από
διαχειριστή.

> Η `app.handle_new_user()` **δεν σηκώνει ποτέ exception**: τρέχει μέσα στη ροή του
> GoTrue και κάθε σφάλμα της γίνεται «Database error querying schema» σε κάθε ενέργεια
> στον `auth.users` — και στην απλή σύνδεση.

Η **δοκιμή 13** του `test_failsafe.sql` κλειδώνει και τα δύο: διατρέχει και τους 66
πίνακες με RLS ως προϊστάμενος και απαιτεί μηδέν σφάλματα policy, και ελέγχει ότι μια
εγγραφή εκτός λίστας δεν αποκτά προφίλ και δεν βλέπει τίποτε. Χωρίς το 0026 η δοκιμή
πέφτει με τα τρία ονόματα πινάκων.

Οι πέντε προειδοποιήσεις `SECURITY DEFINER` του Supabase advisor
(`create_project_full`, `instantiate_workflow`, `next_protocol_no`, `org_people`,
`stage_blockers`) **είναι σκόπιμες**: καθεμία φέρει εσωτερικό έλεγχο δικαιώματος
(`can_approve`/`can_supervise`/`my_org`) και το `anon` δεν τις εκτελεί.

## Τι εκκρεμεί, κατά σειρά

1. Φόρτωση των **πραγματικών προτύπων `.docx`** της Δ/νσης στον κάδο `templates`.
2. Αντικατάσταση του ενδεικτικού `work_groups` με το **παράρτημα της Υ.Α.
   ΔΝΣγ/οικ.38107/ΦΝ 466/2017**, όταν υπάρχει πρόσβαση στο ΦΕΚ.
3. **Απενεργοποίηση δημόσιων εγγραφών** στο Supabase (Authentication → Sign In /
   Providers). Είναι ρύθμιση του πίνακα ελέγχου, δεν αλλάζει από migration· το 0027
   είναι η άμυνα σε βάθος για την περίπτωση που μείνει ανοιχτή.
4. Ενεργοποίηση του **leaked password protection** (Authentication → Passwords) —
   η μόνη προειδοποίηση του advisor που δεν είναι σκόπιμη.
5. Εφαρμογή των migrations **0024–0027** στο ζωντανό project — δεν έχουν εφαρμοστεί.
